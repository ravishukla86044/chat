services:
  mysql:
    image: mysql:8.0
    container_name: dms-mysql
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: chat
      MYSQL_USER: chat
      MYSQL_PASSWORD: chatpass
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-prootpass"]
      interval: 5s
      timeout: 5s
      retries: 20

  app:
    build: .
    container_name: dms-app
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      DB_HOST: mysql
      DB_PORT: "3306"
      DB_NAME: chat
      DB_USER: chat
      DB_PASSWORD: chatpass
      SERVER_PORT: "8080"
    ports:
      - "8080:8080"

  # Run the full test suite inside Docker (no local Java/Maven needed).
  # Uses Testcontainers, which talks to the host Docker daemon via the mounted
  # socket and starts MySQL as a sibling container.
  #   Usage:  docker compose run --rm test
  test:
    image: maven:3.9-eclipse-temurin-21
    working_dir: /app
    volumes:
      - .:/app
      - maven-cache:/root/.m2
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      # On Docker Desktop the test container reaches mapped ports via the host.
      TESTCONTAINERS_HOST_OVERRIDE: host.docker.internal
      TESTCONTAINERS_RYUK_DISABLED: "true"
    command: ["mvn", "-B", "test"]
    profiles: ["test"]

volumes:
  mysql-data:
  maven-cache:
